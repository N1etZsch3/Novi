## 🚀 Novi·挚友：项目结构目录



这是一个基于 **Spring Boot** 的标准Maven/Gradle项目结构。这种结构遵循“关注点分离” (Separation of Concerns) 的原则，易于维护和扩展。

```shell
Novi/
├── .mvn/                   # Maven Wrapper
├── .gitignore              # Git忽略配置
├── mvnw                    # Maven Wrapper (Linux/macOS)
├── mvnw.cmd                # Maven Wrapper (Windows)
├── pom.xml                 # Maven项目配置 (依赖管理)
│
└── src/
    ├── main/
    │   ├── java/
    │   │   └── com/n1etzsch3/novi/    # Java代码根包
    │   │       ├── NoviApplication.java        # Spring Boot 启动类
    │   │       │
    │   │       ├── config/         # 1. 配置层
    │   │       │   ├── AiConfig.java           # Spring AI 配置
    │   │       │   ├── SecurityConfig.java     # Spring Security安全配置
    │   │       │   └── WebConfig.java          # Web相关配置
    │   │       │
    │   │       ├── controller/     # 2. 接口层 (Web API)
    │   │       │   ├── ChatController.java     # 核心聊天API
    │   │       │   ├── UserAccountController.java # 用户账号管理
    │   │       │   └── ChatSessionController.java # 会话管理
    │   │       │
    │   │       ├── service/        # 3. 服务层 (核心业务逻辑)
    │   │       │   ├── ChatService.java        # 聊天流程编排
    │   │       │   ├── UserAccountService.java # 用户账号服务
    │   │       │   ├── AiPromptConfigService.java # 提示词与配置服务
    │   │       │   └── impl/                   # 服务实现类
    │   │       │       ├── ChatServiceImpl.java
    │   │       │       └── ...
    │   │       │
    │   │       ├── repository/     # 4. 数据访问层 (Spring AI ChatMemory)
    │   │       │   ├── NoviDatabaseChatMemory.java # 自定义聊天记忆实现
    │   │       │   └── NoviMemoryRepository.java   # 记忆存储
    │   │       │
    │   │       ├── mapper/         # 5. MyBatis Mapper
    │   │       │   ├── UserAccountMapper.java
    │   │       │   ├── ChatSessionMapper.java
    │   │       │   └── ChatMemoryMapper.java
    │   │       │
    │   │       ├── pojo/           # 6. 领域模型
    │   │       │   ├── entity/                 # 数据库实体
    │   │       │   │   ├── UserAccount.java
    │   │       │   │   ├── ChatSession.java
    │   │       │   │   └── ChatMessage.java
    │   │       │   └── dto/                    # 数据传输对象
    │   │       │       ├── ChatRequest.java
    │   │       │       ├── ChatResponse.java
    │   │       │       └── NoviPersonaSettings.java
    │   │       │
    │   │       └── exception/      # 7. 异常处理
    │   │           └── GlobalExceptionHandler.java
    │   │
    │   └── resources/
    │       ├── application.properties  # 配置文件
    │       ├── mapper/                 # MyBatis XML
    │       └── static/                 # SQL脚本等
    │
    └── test/                   # 测试代码
```



------



## 🧠 整体设计思路与核心流程



"Novi·挚友"的核心价值在于**个性化**和**记忆**。我们的设计必须围绕数据流如何增强这两个点来展开。



### 1. 核心模块详解



1. **配置层 (Config)**:
   - `AiConfig`: Spring AI 的自动配置，通过 `application.properties` 加载 API Key。
2. **接口层 (Controller)**:
   - `ChatController`: 接收 `ChatRequest`，调用 `ChatService`。
3. **数据访问层 (Mapper/Repository)**:
   - 使用 **MyBatis** 进行数据库操作 (`UserAccountMapper`, `ChatSessionMapper`)。
   - `NoviDatabaseChatMemory`: 实现 Spring AI 的 `ChatMemory` 接口，负责将聊天记录持久化到数据库。
4. **领域模型 (Pojo)**:
   - **Entity**: `UserAccount`, `ChatSession`, `ChatMessage`.
   - **DTO**: `ChatRequest`, `ChatResponse`, `NoviPersonaSettings`.
5. **AI客户端 (Spring AI)**:
   - 直接使用 Spring AI 提供的 `ChatClient`，无需手动封装 Adapter。
   - 支持流式 (Stream) 和阻塞式 (Call) 调用。
6. **提示词服务 (AiPromptConfigService)**:
   - 负责管理系统提示词模板、性格描述和语气风格。
   - 支持从数据库动态加载配置。
7. **记忆服务 (MemoryService)**:
   - **当前实现**: 主要依赖 `NoviDatabaseChatMemory` 进行上下文（Window Memory）管理。
   - **未来计划**: 实现基于向量数据库的长期记忆检索和事实提取。



### 2. 核心业务流程：一次聊天 (Chat Flow)



这是用户发送一条消息的完整后端处理流程：

**[用户] -> [API] -> [Controller] -> [Service] -> [AI] -> [Service] -> [Controller] -> [API] -> [用户]**



#### 步骤 1：请求进入 (ChatController)

1. 用户发送 `POST /api/chat/stream` (或 `/call`)。
2. `ChatController` 调用 `chatService.handleStreamMessage(request)`。

#### 步骤 2：系统消息构建 (ChatService)

1. **获取用户与偏好**: 查询 `UserAccount` 和 `UserPreferenceService`。
2. **构建人设**: 根据偏好设置（如 "毒舌"、"温柔"）和 `AiPromptConfigService` 中的模板，生成 System Prompt。
3. **注入环境信息**: 注入当前时间、日期、季节暗示等，增强 AI 的真实感。

#### 步骤 3：会话管理 (ChatService)

1. 检查 `sessionId`。如果是新会话，创建 `ChatSession` 并自动生成标题（截取前20字）。
2. 如果是旧会话，更新最后活跃时间。

#### 步骤 4：调用 AI (Spring AI ChatClient)

1. `ChatService` 构建 `Prompt`，包含 System Message 和 User Message。
2. **注入记忆**: 使用 `NoviDatabaseChatMemory`，Spring AI 会自动将之前的对话历史（作为 Advisor）附加到 Prompt 中。
3. 调用 `chatClient.prompt().stream()` 发起请求。

#### 步骤 5：流式响应与持久化

1. AI 返回流式数据 (`Flux<String>`)。
2. `ChatService` 将内容包装为 `StreamEvent` 并序列化为 JSON 返回给前端。
3. **自动持久化**: `NoviDatabaseChatMemory` 会拦截对话结束信号，将 User Message 和 Assistant Response 保存到 `chat_message` 表中。

#### 步骤 6：【TODO】记忆提取 (MemoryService)

> ⚠️ 此功能尚未实装，计划在未来版本中添加。

1. 异步分析对话内容。
2. 提取关键事实（如用户喜好、重要日期）。
3. 存入 `user_memory` 表供下次检索。



### 3. 数据库设计 (Model/Entity)



- **User**: `id`, `username`, `email`, `nickname` (昵称), `preferences` (JSON/String，存储用户偏好，比如Novi的性格)。
- **ChatMessage**: `id`, `userId` (关联User), `role` ("user" 或 "assistant"), `content` (消息内容), `timestamp`。
- **Memory**: `id`, `userId` (关联User), `fact` (事实，例如 "用户的生日是10月1日"), `type` (类型，例如 "Birthday", "Family", "Pet"), `lastAccessTime` (用于LRU缓存或检索)。


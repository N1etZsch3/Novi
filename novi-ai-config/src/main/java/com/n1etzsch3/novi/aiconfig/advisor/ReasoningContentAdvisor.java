package com.n1etzsch3.novi.aiconfig.advisor;

// TEMPORARILY DISABLED: Spring AI 1.1.x advisor API classes are not available
// in the current dependency configuration. Will re-enable when correct dependency is found.
// See: org.springframework.ai.chat.client.advisor.api.{AdvisedRequest, AdvisedResponse, CallAroundAdvisor, CallAroundAdvisorChain}

/*
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.ai.chat.client.advisor.api.AdvisedRequest;
import org.springframework.ai.chat.client.advisor.api.AdvisedResponse;
import org.springframework.ai.chat.client.advisor.api.CallAroundAdvisor;
import org.springframework.ai.chat.client.advisor.api.CallAroundAdvisorChain;
import org.springframework.ai.chat.messages.AssistantMessage;
import org.springframework.ai.chat.metadata.ChatGenerationMetadata;
import org.springframework.ai.chat.model.ChatResponse;
import org.springframework.ai.chat.model.Generation;
import org.springframework.util.StringUtils;

import java.util.Collections;
import java.util.Map;
import java.util.Objects;
*/

/**
 * Advisor to extract and process DashScope/DeepSeek reasoning content.
 * 
 * TEMPORARILY DISABLED due to Spring AI 1.1.x dependency compatibility issues.
 * The advisor API classes (AdvisedRequest, AdvisedResponse, CallAroundAdvisor,
 * CallAroundAdvisorChain)
 * are not available in the current Spring AI Maven artifacts.
 */
public class ReasoningContentAdvisor {
    // Temporarily disabled - see class Javadoc for details
}
